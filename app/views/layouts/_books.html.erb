<div class="books col-md-8 offset-1">
  <h2>Books</h2>
  <table class="table table-hover">
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
      <tr>
        <% if book.user.profile_image.present? %>
          <td><%= attachment_image_tag book.user, :profile_image, size: '50x50', format: 'jpeg' %></td>
        <% else %>
          <td><%= image_tag 'no-image-icon.jpg' %></td>
        <% end %>
        <td><%= link_to "#{book.title}", book_path(book) %></td>
        <td><%= book.body %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
